{
  "name": "collectorset",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "format": "prettier --write \"**/*.{json,js,ts,html,css,scss,md,yaml,yml,tmpl}\" || true",
    "format:check": "prettier --check \"**/*.{json,js,ts,html,css,scss,md,yaml,yml,tmpl}\" || true",
    "format:safe": "prettier --write \"**/*.{json,js,ts,html,css,scss,md,yaml,yml}\""
  },
  "devDependencies": {
    "prettier": "^3.3.2"
  }
}
