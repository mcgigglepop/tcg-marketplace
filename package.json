{
  "name": "collectorset-lambdas",
  "type": "module",
  "private": true,
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "node scripts/build.mjs",
    "zip": "node scripts/zip.mjs",
    "package": "npm run build && npm run zip"
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "zip-a-folder": "^3.1.6"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.624.0",
    "@aws-sdk/lib-dynamodb": "^3.624.0"
  }
}
